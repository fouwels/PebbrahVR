<Page
    x:Class="PebbrahRT2.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PebbrahRT2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock x:Name="Status"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Margin="10"/>
        <CaptureElement x:Name="CaptureElement" Stretch="Fill"/>
        
        <Grid x:Name="Overlay">
            <Grid.ColumnDefinitions>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="100"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <StackPanel x:Name="DataStack" Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center" Background="Gray" Opacity="0.5" >
                <TextBlock x:Name="Thing1" FontSize="48" Text="Null" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="Thing2" FontSize="48" Text="Null" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
